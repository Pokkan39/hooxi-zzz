const pageKey=document.body.classList.contains('subpage')?location.pathname.split('/').pop().replace('.html',''):'mainline';const pageData=(window.archiveData&&archiveData[pageKey])||[];const pageStoreKey=`hooxi:${pageKey}`;let items=loadPageItems();const escPage=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function loadPageItems(){try{return JSON.parse(localStorage.getItem(pageStoreKey))||pageData.map(x=>({...x}))}catch{return pageData.map(x=>({...x}))}}function persist(){localStorage.setItem(pageStoreKey,JSON.stringify(items));toast('排序已保存到本机浏览器')}function renderPage(){const box=document.querySelector('#pageTimeline');document.querySelector('#pageCount').textContent=items.length;box.innerHTML=items.map((x,i)=>`<article class="page-timeline-item" draggable="true" data-id="${escPage(x.id)}"><div class="page-node">${String(i+1).padStart(2,'0')}</div><div class="page-card"><span class="episode-tag">${escPage(x.tag)}</span><h2>${escPage(x.title)}</h2><p>${escPage(x.summary)}</p><div class="page-links">${x.video?`<a class="video-link" href="${escPage(x.video)}" target="_blank" rel="noreferrer">▶ 播放视频 ↗</a>`:'<span class="video-link disabled">◌ 视频待接入</span>'}<a class="wiki-link" href="${escPage(x.wikiUrl)}" target="_blank" rel="noreferrer">官方资料 ↗</a></div></div><button class="drag-handle" title="拖动排序" aria-label="拖动排序">⠿</button></article>`).join('');let dragging=null;box.querySelectorAll('.page-timeline-item').forEach(el=>{el.addEventListener('dragstart',()=>{dragging=el;el.classList.add('dragging')});el.addEventListener('dragend',()=>{el.classList.remove('dragging');dragging=null});el.addEventListener('dragover',e=>{e.preventDefault();if(dragging&&dragging!==el){const rect=el.getBoundingClientRect();el.parentNode.insertBefore(dragging,e.clientY<rect.top+rect.height/2?el:el.nextSibling)}});el.addEventListener('drop',()=>{items=[...box.querySelectorAll('.page-timeline-item')].map(node=>items.find(x=>x.id===node.dataset.id)).filter(Boolean).map((x,i)=>({...x,order:i+1}));persist();renderPage()})})}renderPage();document.querySelector('#sortReset').onclick=()=>{items=pageData.map(x=>({...x}));persist();renderPage()};
