# Hooxi // 绝区零剧情档案

Hooxi 的《绝区零》剧情视频档案站：以新艾利都工业街头风格整理主线剧情、角色剧情分支、角色专属 EP / PV 与主题活动。

## 在线访问

GitHub Pages 地址将在仓库启用 Pages 后生成：

`https://pokkan39.github.io/hooxi-zzz/`

## 小白编辑

打开网站右上角的黄色 `✦`，进入站内编辑器：

- **外观**：修改标题、介绍、背景图片、透明度、动态粒子和网点效果。
- **内容**：新增、修改、删除剧情卡片，填写分类、标签、标题、简介和视频链接。
- **父级分组**：在档案页里创建更大的章节父级，把 1-4 这类记录收进同一组，并支持展开或收纳。
- **装饰图片**：给档案页添加独立图片贴片，再用左下角“调整位置”拖到页面任意视觉位置。
- **歌单**：填写有授权的 MP3 / OGG 地址，使用底部播放器切换歌曲和调整音量。
- **导出配置 / 导入配置**：用 JSON 备份或迁移站点内容。
- **恢复默认**：清除浏览器本地配置并恢复示例数据。

配置只保存在当前浏览器的 `localStorage`。输入内容会在当前页面即时预览，点击保存后写入本机浏览器；要让访客看到更新，请从编辑器导出 `data.js` / `layout-data.js`，覆盖仓库文件后手动提交并 `git push` 到 GitHub Pages。

## 动态 UI

网站新增了一套克制的交互动效：鼠标悬停卡片时会根据指针产生低幅度光泽和倾斜，按钮有扫光、按下反馈和焦点环，时间轴节点会呼吸提示，页面内容会错峰进入，编辑器和歌单弹层会平滑出现。触摸设备不会强行使用 3D 倾斜，并支持系统的“减少动态效果”设置。


- `mainline.html`：剧情主线时间轴
- `stories.html`：支线与角色故事时间轴
- `behind-scenes.html`：系列·幕后与对谈档案
- `events.html`：主题活动时间轴

时间轴卡片支持拖动排序；编辑时当前页面即时预览，保存后写入当前浏览器。右上角编辑器用于维护相应页面内容、父级分组和装饰图片；左下角“调整位置”可拖动卡片、分组、装饰图片等元素。配置可导入导出作为备份，发布时由用户手动提交并推送仓库。

## 音频文件发布

编辑器的“歌单”页支持直接选择本地音频文件并立即试听。由于 GitHub Pages 是静态托管，网页不能直接把文件写回 GitHub。要让所有访客听到音频：

1. 在编辑器选择音频文件。
2. 将授权音频复制到仓库的 `assets/audio/` 目录。
3. 在编辑器点击“生成发布清单 playlist.json”。
4. 将清单中的地址改为 `assets/audio/文件名.mp3`。
5. 把音频和 `playlist.json` 提交到 GitHub，重新部署后所有访客都能播放。

不要在公开网页中放 GitHub Token。请只发布自己创作或取得授权的音频。


```bash
python -m http.server 8080
```

然后访问 <http://localhost:8080/>。

## 内容来源与版权

绝区零资料分类参考米哈游官方 Wiki：<https://baike.mihoyo.com/zzz/wiki/>。站内使用整理后的原创摘要，并建议通过链接跳转官方页面；请勿未经授权复制官方原文、图片、音乐或视频。

播放器仅应使用你创作、拥有授权或明确允许使用的音频地址。浏览器可能阻止带声音的自动播放，遇到提示时点击底部播放按钮即可启用。

## 项目文件

- `index.html`：网站页面和编辑器结构
- `styles.css`：视觉样式、动态背景和响应式布局
- `app.js`：示例数据、编辑器、本地配置存储和播放器逻辑
- `layout-editor.js`：全站位置编辑器，本地保存响应式布局，并可导出 `layout-data.js`
- `layout-data.js`：公开发布用布局数据，让访客看到相同排版
- `faction.html` / `faction.js`：阵营专属页面
- `docs/README.md`：详细维护说明
