# Hooxi 绝区零剧情档案站

## 当前版本

这是一个不依赖构建工具的静态网站，包含绝区零主题路线图、章节展开/折叠、动态背景、站内音乐播放器和“小白可用”的可视化编辑器。内容栏目为：主线剧情、角色剧情分支、系列·幕后/对谈、主题活动。站内条目是 Hooxi 档案的可编辑示例，不代表 Hooxi 的真实投稿。

## 小白编辑流程

1. 打开网站，点击右上角黄色 `✦`，进入“站点编辑器”。
2. **外观**：修改主标题、首页介绍、背景图片 URL、透明度和动态效果开关；点击“应用外观”即可立即生效。
3. **内容**：修改已有剧情卡片的分类和标题，或点击“新增剧情卡片”。保存后路线图立即刷新。视频地址可在 `app.js` 的数据中继续补充，或后续扩展编辑字段。
4. **父级分组与装饰图片**：四个档案页的编辑器可以新增父级分组，把多条记录收进同一章节；也可以新增独立装饰图片，再用“调整位置”拖动摆放。
5. **歌单**：填写歌曲名称和你有权使用的 MP3/OGG 地址；保存后底部播放器可以播放、切换、调节音量。
6. **导出配置**：把当前所有内容保存为 JSON 备份；换电脑时用“导入配置”恢复。需要回到初始状态时点击“恢复默认”。

编辑内容只保存在当前浏览器：输入时当前页面即时预览，点击保存后写入本机 `localStorage`。导入/导出 JSON 可作为本地备份；要让所有访客看到更新，需要导出发布数据，覆盖仓库文件并手动 `git push` 到 GitHub Pages。

## 官方 Wiki 内容来源

基础栏目参考：<https://wiki.biligame.com/zzz/首页>。本站只整理栏目名称和原创短摘要，并链接回官方 Wiki，不批量复制 Wiki 的原文或图片。

## 音乐与自动播放说明

播放器不会在页面加载时自动播放，也不会因编辑器、卡片或菜单操作而启动。只有点击底部播放按钮才会播放；默认音量为 25%，用户调整后会保存在当前浏览器。请只使用自己创作、获得授权或明确允许使用的音频地址。

## 词条封面、角色立绘与 Wiki 字段

四个档案页的封面现在支持点击跳转视频：词条同时填写“封面图片”和“视频链接”后，访客点击封面会在新标签页打开视频。封面图片建议放到 `assets/covers/`，角色头像/立绘建议放到 `assets/portraits/`，编辑器中填写相对路径。

编辑器按官方 Wiki 常见的信息组织方式分为：基本信息（分类、类型、标题、简介、版本、日期）、角色与世界观（阵营、相关角色、地点/空洞区域）、媒体与来源（视频、封面、角色立绘、附加图片、官方资料链接）。这些字段是结构化预留，不会自动复制 Wiki 原文；可参考：

- <https://wiki.biligame.com/zzz/游戏历史>
- <https://zenless-zone-zero.fandom.com/zh/wiki/绝區零_Wiki>

选择本地图片按钮只提供当前浏览器预览，静态 GitHub Pages 不会自动上传文件。要让所有访客看到，仍需把图片复制到对应资源目录，并在字段中填写路径后导出 `data.js` 发布。图片只保存路径，使用懒加载，适合后续增加数百张图片；封面采用完整展示模式，不会裁切横图或竖图主体。装饰图片同样只保存路径：可在页面编辑器里添加为独立贴片，再用左下角“调整位置”拖动到任意视觉位置。


网站现在提供四个独立档案页：主线时间轴、支线/角色故事、系列·幕后/对谈、主题活动。原 EP/PV 独立页面已移除，相关媒体可以归入角色资料、主线词条或内容块。每个页面的卡片都可以用鼠标拖动重新排序，排序保存在当前浏览器；`data.js` 是默认数据源。

四个时间轴页面右上角的 `✦` 都有独立页面编辑器，可以修改标签、标题、简介和视频地址，也可以新增、删除条目。编辑器还支持创建父级分组：例如把 1-4 条记录放入“PV 与制作记录”这样的更大章节，访客可点击章节头展开或收纳。B 站视频地址支持直接粘贴完整链接或 BV 号；页面会尝试读取公开视频封面，若接口受限则显示提示，但仍可点击链接打开 B 站。

### 让所有访客看到编辑结果

1. 在任意一个时间轴页面打开右上角 `✦` 编辑器。
2. 编辑内容后点击“导出发布数据 data.js”。该文件包含四个页面的完整数据、父级分组和装饰图片路径。
3. 如果调整过页面位置，点击左下角“导出布局”，下载 `layout-data.js`。
4. 用下载的 `data.js` / `layout-data.js` 覆盖仓库根目录的同名文件。
5. 提交并推送到 GitHub，等待 GitHub Pages 部署完成，所有访客就能看到新内容。
6. 以后可在编辑器中选择“导入 data.js / JSON”恢复数据；导入只更新当前浏览器，仍需再次导出并发布才能同步给访客。

编辑内容保存在当前浏览器的 `localStorage`。静态 GitHub Pages 不会让网页直接写回仓库；发布更新时，请用编辑器导出 `data.js` / `layout-data.js`，覆盖仓库文件后手动提交并推送到 GitHub。不要在公开网页中放 GitHub Token。

## 全站位置编辑

全站页面左下角提供“调整位置”工具。首页、菜单、页面标题、父级分组、时间线、剧情卡片、封面、文字区、装饰图片、播放器和页脚可以拖动；选中元素后可调整宽度与层级。桌面、平板、手机使用独立布局数据，点击“恢复当前设备”只清除当前断点，不影响其他设备。布局保存在当前浏览器，点击“导出布局”可生成 `layout-data.js`；覆盖仓库同名文件后，访客会看到相同排版。

## 阵营专属页面

词条填写稳定的 `factionId` 后，标题和“进入阵营档案”会跳转到 `faction.html?id=<factionId>`。阵营定义位于 `data.js` 的 `factions` 集合，支持名称、简介、标识、背景和主题色；专属页会自动汇总主线、支线、幕后/对谈和活动中的相关记录。视频封面仍优先打开视频，不与阵营入口冲突。


## 单人本地编辑与发布

网站已回退为单人本地编辑模式：

- 首页、四个档案页、播放器歌单和全站位置布局都只写入当前浏览器。
- 保存按钮不会调用云端 API，也不会把内容自动同步给其他访客。
- 需要公开发布时，从编辑器导出发布数据，覆盖仓库中的 `data.js` / `layout-data.js` 或相关资源文件。
- 完成文件替换后，由维护者手动提交并 `git push`，等待 GitHub Pages 更新。
- 公开分享链接为 `https://pokkan39.github.io/hooxi-zzz/`。


在站内编辑器的“歌单”标签中选择本地音频，可以立即在当前浏览器试听。若要让 GitHub Pages 的所有访客都能听到：将授权音频复制到仓库 `assets/audio/`，点击“生成发布清单 playlist.json”，把清单中的地址改成 `assets/audio/文件名.mp3`，然后把音频和 JSON 一起提交到 GitHub。纯静态 GitHub Pages 不能让网页直接写入仓库，因此不能在公开网页中放 GitHub Token。


在项目目录执行：

```bash
python -m http.server 8080
```

然后访问 `http://localhost:8080/`。
