# S1 视觉与信息架构重构说明

> 日期：2026-07-19  
> 策略：对照文档默认 **S1 档案纯化**（有货再开、不装假模块）  
> Skill 取向：Hallmark（Signal Workbench / midnight tape desk）+ UI 可读性

## 目标

1. 解决「难看 / 不协调 / 配色脏」：停 pass-N 覆盖层，改为 `tokens.css` + 干净主题层。  
2. 解决「功能模块假空」：首页去掉 LOCK 图鉴宫格与重复优先入口墙。  
3. 保留双门：查档主路径 + 可选录像店。

## 首页新 IA

1. 英雄区：任务文案 + 开始查档 / 进入录像店  
2. **三条查档路径**：主线补课 · 角色与阵营 · 按名字找人  
3. **次要 chip**：角色剧情 / 活动 / 幕后 / 录像店（不占主栅格）  
4. 代理人精选  
5. 剧情分栏（主线 / 角色剧情 / 活动）  
6. 关于

明确不进主路径：音擎、邦布图鉴、驱动盘、地图（产品书未升格）。

## 视觉 token（摘要）

| 角色 | 值 |
|------|-----|
| 舞台底 | `#0e1014` |
| 面板阶 | `#151922` → `#1c2230` → `#262e40` |
| 正文 | `#f4f1ea` |
| 主强调 | `#ffc107`（琥珀，点缀 ≤ 关键面积） |
| 次强调 | `#ff7a45` / `#5ee0c4` |

## 改动文件

- `tokens.css`（新）
- `theme-zzz.css`、`motion.css`、`index.html`、`app.js`
- 子页缓存：`mainline/stories/character/faction/events/behind-scenes`
- 闸门：`scripts/check-formal-site-gate.py`、`artifacts/formal-site-gate-baseline.json`

## 已知未决

- 主线/活动/幕后**内容厚度**仍薄（数据问题，非再铺瓷砖可解）。  
- 若你要 S2（轻量图鉴壳）或更强 HUD/电影感，在 S1 底子上改浓度即可。  
- 子页导航文案尚未与首页完全统一精简（下一轮可齐）。

## S1.7 代理人名单交互修正

- 角色名单改为统一 18% 斜切轮廓，并用列高差与逐行横移组成三条同向平行轨道；`<=1180px` 自动收敛为双轨，`<=880px` 保留卡片斜切但取消桌面覆盖接缝。
- SELECT 侧轨、选中卡、舞台信号和滚动条统一读取当前代理人主题色，不再使用固定荧光绿。
- 正常动效模式仅在鼠标输入和角色切换时驱动舞台、卡内肖像与 wipe；减少动效模式保留颜色、文字、焦点和选中态，但取消空间位移。
- 本地正式预览固定从仓库根目录启动，避免旧副本继续提供过期 CSS/JS。

## 验证

- 浏览器抽检首页路径 + 主线车道  
- `node --check app.js`  
- `node scripts/regression.mjs`：角色名单 38 项视觉、交互、减动效与响应式检查通过。  
- 正式站闸门基线已重写  
